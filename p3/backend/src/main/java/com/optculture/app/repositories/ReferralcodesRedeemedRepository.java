package com.optculture.app.repositories;

import com.optculture.shared.entities.promotion.ReferralcodesRedeemed;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ReferralcodesRedeemedRepository extends JpaRepository<ReferralcodesRedeemed,Long> {
}
